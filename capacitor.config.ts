import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.theblackboyheal.blackboyheal',
  appName: 'Black Boy Heal',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
